{
  "rules": {
    ".read": false,
    ".write": false,
    "boards": {
      "$boardId": {
        ".read": "data.child('users').child(auth.uid).val() !== null"
      }
    },
    "users": {
      "$userId": {
        ".read": "$userId === auth.uid"
      }
    }
  }
}
